{
  "name": "agent-backlog-monorepo",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/ui",
    "packages/api",
    "packages/mcp"
  ],
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.18",
    "vitest": "^4.0.18"
  }
}
