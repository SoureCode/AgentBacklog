{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd ${CLAUDE_PLUGIN_ROOT}/mcp && [ -d node_modules ] || npm install --no-audit --no-fund"
          }
        ]
      }
    ]
  }
}
